# tstudio-viewer

T-Studio 매매 보고서 뷰어의 껍데기(HTML 한 장). 자료는 이 저장소에 없다 — 열면 로그인 카드가 뜨고,
승인된 계정으로 로그인해야 Supabase 함수가 권한을 보고 자료를 내준다. 원본은 TradingStudioPlus 저장소의
`tools/render-report-viewer.py`가 만들고 `tools/upload-report-viewer.py --web-out`이 여기로 낸다. 손으로 고치지 않는다.
